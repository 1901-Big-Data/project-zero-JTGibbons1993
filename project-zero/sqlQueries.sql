CREATE TABLE LockedUser(
    accountL int,
    lockedName varchar2(50), 
    lockedpassword varchar2(50), 
    lockedssn int, 
    lockedtype varchar2(50));
    
CREATE TABLE ACCOUNTA(
    accountNumber int, 
    accountBalance int);
